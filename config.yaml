data:
  img_size: 224
  batch_size: 32
  val_split: 0.15
  test_split: 0.15
  num_workers: 4
  seed: 42

model:
  backbone: efficientnet_b0
  pretrained: true

train:
  epochs: 15
  lr: 0.0003
  weight_decay: 0.0001
  label_smoothing: 0.1

paths:
  checkpoint_dir: checkpoints
  output_dir: outputs
